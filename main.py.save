r = int(requests.args.get("r"))import flask
from flask import Flask
from flask import request
import json

import board
import neopixel


app = Flask(__name__)
app.config['DEBUG'] = False
app.config['ENV'] = 'development'

pixels = neopixel.NeoPixel(board.D18, 30*2)

pixels.fill((0, 255, 0))
#pixels[1] = (255, 0, 0)


@app.route('/solid')
def solid:
	
	
